# This repo builds Linux containers, and its shell scripts (rootfs/) run
# inside them. CRLF shebangs fail with "not found" there. Force LF for all
# text files regardless of the checkout platform's core.autocrlf.
* text=auto eol=lf

# Keep real binaries untouched.
*.wad binary
*.png binary
*.ico binary
*.wasm binary
